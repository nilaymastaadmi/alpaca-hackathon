.env
.env.*
.venv/
__pycache__/
*.pyc

backtest/cache/option_bars.csv
backtest/cache/*.csv

artifacts/state.json
artifacts/watchdog.json
artifacts/watchdog.log

# D3 comparison dry-runs (agent/agent.py --compare): internal decision-support
# data for choosing between T4/T6/T7, not the real submission's artifact
# trail. Keeping these out of the repo so a judge browsing artifacts/ only
# ever finds the real deployed agent's decisions.
artifacts/compare/

# keep-awake task output, local only
prep/wake.log
prep/wake_task.log
prep/flatten_watch.log
node_modules/
package-lock.json
